---
import '../styles/global.css';
import { getSiteConfig } from '../config/site-config';

interface Props { title?: string; description?: string; noindex?: boolean }
const siteConfig = getSiteConfig(Astro.url.pathname);
const { title = siteConfig.seo.title, description = siteConfig.seo.description, noindex = false } = Astro.props;
const siteUrl = Astro.site ?? new URL('https://example.com');
const canonical = new URL(Astro.url.pathname, siteUrl);
const baseUrl = import.meta.env.BASE_URL;
const language = Astro.url.pathname.includes('/en/') ? 'en' : 'uk';
const socialImage = new URL(`${baseUrl}/${siteConfig.seo.image.replace(/^\//, '')}`, siteUrl);
const faviconUrl = `${baseUrl}/favicon.svg`;
---

<!doctype html>
<html lang={language}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={description} />
    <meta name="theme-color" content="#e8673d" />
    {noindex && <meta name="robots" content="noindex" />}
    <link rel="canonical" href={canonical} />
    <link rel="icon" href={faviconUrl} type="image/svg+xml" />
    <meta property="og:type" content="website" />
    <meta property="og:locale" content={language === 'en' ? 'en_US' : 'uk_UA'} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={socialImage} />
    <meta name="twitter:card" content="summary_large_image" />
    <title>{title}</title>
  </head>
  <body>
    <a class="skip-link" href="#main">Перейти до основного вмісту</a>
    <slot />
  </body>
</html>
